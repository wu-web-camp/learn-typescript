// 3. Control Flow: if / else  |  ข้อ 10 - 12  |  Session 1
//
// ให้โปรแกรม "ตัดสินใจ" ตามเงื่อนไข

const score: number = 85;

// ข้อ 10: if / else if / else
// TODO: Implement ข้อ 10
// 1. ถ้า score >= 90 print "Excellent"
// 2. ถ้าไม่ใช่ แต่ >= 80 print "Good"
// 3. นอกนั้น print "Keep practising"
// คำใบ้: else if ตรวจต่อเมื่อเงื่อนไขก่อนหน้าไม่จริง -- ลำดับจึงสำคัญ

// ข้อ 11: ลองเปลี่ยนค่า
// TODO: Implement ข้อ 11
// 1. เปลี่ยน score เป็น 95 แล้ว 70 แล้วรันดูทีละครั้ง
// 2. ทายผลก่อนรันทุกครั้ง

// ข้อ 12: union type + typeof
// TODO: Implement ข้อ 12
// 1. ประกาศ let input: number | string = 15;
//    เครื่องหมาย | อ่านว่า "หรือ" -- ตัวแปรนี้เป็นตัวเลข "หรือ" ข้อความก็ได้
// 2. เขียน if (typeof input === "number") { ... } else { ... }
// 3. print ให้บอกได้ว่าที่ใส่มาเป็นเลขหรือข้อความ
// 4. ลองเปลี่ยน input เป็น "TypeScript" แล้วรันใหม่

// ✅ CHECKPOINT: เขียน if / else if / else ได้ และอธิบายได้ว่า number | string แปลว่าอะไร
