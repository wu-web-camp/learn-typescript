{
  "name": "learn-typescript",
  "version": "1.0.0",
  "private": true,
  "description": "Code-along sessions for learning TypeScript from scratch",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.exercise.json",
    "check:solutions": "tsc --noEmit -p tsconfig.check.json",
    "watch": "tsx watch"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
