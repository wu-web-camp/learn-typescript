// 5. Array (ลิสต์)  |  ข้อ 17 - 31  |  Session 2
//
// วงเล็บเหลี่ยม [ ] แปลว่า "ลิสต์" คั่นแต่ละตัวด้วยลูกน้ำ
// ลิสต์รู้ว่าตัวเองเก็บอะไร: string[] = ลิสต์ของข้อความ, number[] = ลิสต์ของตัวเลข

// ---------- สร้างลิสต์ (ข้อ 17 - 19) ----------

// ข้อ 17: สร้าง array ของ string
// TODO: Implement ข้อ 17
// 1. สร้าง fruits เก็บ "apple", "banana", "cherry" แล้ว print
// คำใบ้: const fruits: string[] = [...];

// ข้อ 18: สร้าง array ของ number
// TODO: Implement ข้อ 18
// 1. สร้าง scores เก็บ 90, 85, 100, 72 แล้ว print
// คำใบ้: เอาเมาส์ไปชี้ที่ scores จะเห็นว่าเป็น number[]

// ข้อ 19: ลองให้ TypeScript ฟ้อง (ทำแล้วลบทิ้ง)
// TODO: Implement ข้อ 19
// 1. เขียน scores.push("oops");
// 2. อ่าน error แล้วลบบรรทัดนั้นทิ้ง
// สังเกต: โค้ดยังรันได้ เพราะ tsx ไม่เช็ค type ให้ -- เส้นแดงต่างหากที่บอกเรา

// ---------- อ่านด้วย index (ข้อ 20 - 23) ----------
// นับเริ่มที่ 0 ตัวแรกอยู่ index 0 และ index ตัวสุดท้าย = length - 1

// ข้อ 20: อ่านทีละตัว
// TODO: Implement ข้อ 20
// 1. print fruits[0], fruits[1], fruits[2]
// 2. อ่านว่า "fruits ตำแหน่ง 0" -- ตัวแรกคือ 0 ไม่ใช่ 1

// ข้อ 21: อ่านเลยขอบลิสต์
// TODO: Implement ข้อ 21
// 1. ทายก่อนว่า fruits[5] ได้อะไร แล้วค่อย print
// 2. ได้ undefined = "ไม่มีอะไรตรงนั้น" ไม่ได้พัง

// ข้อ 22: ลิสต์ยาวเท่าไร
// TODO: Implement ข้อ 22
// 1. print fruits.length
// คำใบ้: ไม่มีวงเล็บ เพราะเป็นข้อเท็จจริง ไม่ใช่การกระทำ

// ข้อ 23: หยิบตัวสุดท้าย
// TODO: Implement ข้อ 23
// 1. print ตัวสุดท้ายโดยไม่เขียนเลขตรงๆ
// คำใบ้: fruits[fruits.length - 1]

// ---------- แก้ไขลิสต์ (ข้อ 24 - 27) ----------

// ข้อ 24: เพิ่มท้ายลิสต์
// TODO: Implement ข้อ 24
// 1. ใช้ push() เพิ่มผลไม้อีกหนึ่งอย่าง แล้ว print
// คำใบ้: push เป็นการกระทำ จึงมีวงเล็บ (ต่างจาก .length)

// ข้อ 25: แทนที่ด้วย index
// TODO: Implement ข้อ 25
// 1. เปลี่ยนตัวที่ตำแหน่ง 1 เป็น "blueberry"
// คำใบ้: เหลี่ยมอันเดิม แต่ย้ายไปซ้ายของ = -- ซ้ายคือเขียน ขวาคืออ่าน

// ข้อ 26: ลบตัวท้ายสุด
// TODO: Implement ข้อ 26
// 1. ใช้ pop() แล้วเก็บค่าที่ได้ไว้ในตัวแปร removed
// 2. print ทั้งลิสต์และ removed

// ข้อ 27: const กับลิสต์
// TODO: Implement ข้อ 27
// 1. const colors = ["red", "green"];
// 2. colors.push("blue");  <- ทำได้!
// 3. ลอง colors = ["totally", "new"];  <- ฟ้อง แล้วลบทิ้ง
// จำไว้: const ล็อกกล่อง ไม่ได้ล็อกของข้างใน

// ---------- วนลูปในลิสต์ (ข้อ 28 - 31) ----------

// ข้อ 28: ลูปแบบใช้ index
// TODO: Implement ข้อ 28
// 1. วน scores แล้ว print ทุกตัว
// คำใบ้: for (let i = 0; i < scores.length; i++)
// ระวัง: เริ่มที่ 0 และใช้ < ไม่ใช่ <= (ไม่งั้นเกินไป 1 แล้วได้ undefined)

// ข้อ 29: บวกสะสม
// TODO: Implement ข้อ 29
// 1. เอาแพตเทิร์นจากข้อ 14 มาใช้ แต่คราวนี้ตัวเลขมาจากลิสต์
// 2. print ผลรวม -- ควรได้ 347

// ข้อ 30: ค่าเฉลี่ย
// TODO: Implement ข้อ 30
// 1. print ผลรวมหารจำนวนตัว -- ควรได้ 86.75

// ข้อ 31: ลูปแบบ for...of
// TODO: Implement ข้อ 31
// 1. วน fruits ด้วย for (const fruit of fruits) แล้ว print
// คำใบ้: แบบนี้ fruit "คือตัวของ" เลย ไม่ต้องใช้ [i] ไม่ต้องห่วง index
// ใช้ index loop เมื่อต้องรู้ตำแหน่ง / ใช้ for...of เมื่อต้องการแค่ตัวของ

// ✅ CHECKPOINT: สร้างลิสต์ได้, หยิบตัวแรก/ตัวสุดท้ายได้, push/pop/แทนที่ได้,
//    และวนลูปหาผลรวมได้ทั้งสองแบบ
